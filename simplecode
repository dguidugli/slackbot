#para iniciar mensagem ao usuário pelo slack
#para funcionar precisa estar instalado antes o client do slack pelo terminal "pip install slackclient"
#commit realizado pelo jupyter notebook
#abrir o jupyter notebook pelo terminal

import slack

SLACK_TOKEN="<BOT_USER_OAUTH_TOKEN>"

client = slack.WebClient(token=SLACK_TOKEN)
result = client.chat_postMessage(channel='ID_SLACK',text='Hello :wave:') #apenas aceita emoji padrão do Slack
